inherit: base

model:
  d_model: 512
  n_heads: 8
  n_encoder_layers: 6
  n_decoder_layers: 6
  latent_dim: 512
  num_queries: 4  # Multi-query compression (4 queries like PhraseVAE)
  training_mode: "vae"
  kl_reduction: "per_dim"  # or "per_seq" for PhraseVAE-style
