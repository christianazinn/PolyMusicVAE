# Stage 3: Compress latent space from 256 -> 128
# Continue with frozen encoder
#
# Start from Stage 2 checkpoint

inherit: default_512
name: "staged_3_compress_128"

data:
  ds_path: "/home/christian/vae/data_nb_1_combined"

model:
  latent_dim: 128
  num_queries: 4
  training_mode: "ae"
  beta_end: 0.0
  free_bits: null

trainer:
  max_epochs: 2

staged_training:
  enabled: true
  source_run_id: "staged_2_compress_256"
  new_latent_dim: 128
  training_mode: "ae"
  freeze_encoder: true
  freeze_decoder: false
